"use strict";
let users = ["ayan", "irtesam", "yasha", "isra",];
if (users.length === 0) {
    console.log("we need to find some users!");
}
else {
    users = [];
    console.log("All users has been removed" + users.length);
}
